# List of users and passwords
$users = @(
    @{Username="nacer_boumergoud"; Password="Acs_nacer2026"},
    @{Username="brahim_irakhsissen"; Password="Acs_bra11!"},
    @{Username="saad_bengrich"; Password="$aad_Acs!!"},
    @{Username="meryem_chaker"; Password="Mery_Acs22"},
    @{Username="mohammed_erramiqi"; Password="Med_Acs99"},
    @{Username="soukaina_Hanane"; Password="$ou_Acs!1"}
)

foreach ($user in $users) {
    $username = $user.Username
    $password = $user.Password

    # Convert password to secure string
    $securePassword = ConvertTo-SecureString $password -AsPlainText -Force

    # Create local user
    New-LocalUser -Name $username -Password $securePassword -FullName $username -Description "Created via script" -ErrorAction SilentlyContinue

    # Add to Administrators group
    Add-LocalGroupMember -Group "Administrators" -Member $username -ErrorAction SilentlyContinue

    # Add to Remote Desktop Users group
    Add-LocalGroupMember -Group "Remote Desktop Users" -Member $username -ErrorAction SilentlyContinue

    # Set password to never expire
    Set-LocalUser -Name $username -PasswordNeverExpires $true

    Write-Host "User $username created and configured successfully"
}
